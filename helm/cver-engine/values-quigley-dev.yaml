# Quigely dev values for cver-engine

replicaCount: 1

nameOverride: "dev"

app:
  logLevel: "INFO"
  envName: "dev"
  debug: true
  engineDownloadLimit: 3
  engineScanLimit: 3

test:
  test: true

image:
  repository: harbor.squid-ink.us/cver/cver-engine
  pullPolicy: Always
  tag: "dev-latest"

imagePullSecrets:
  - name: harbor

# Quigely dev values for cver-engine

replicaCount: 1

nameOverride: "dev"

app:
  logLevel: "INFO"
  envName: "dev"
  debug: true

test:
  test: true

image:
  repository: harbor.squid-ink.us/politeauthority/cver-engine
  pullPolicy: Always
  tag: "dev-latest"

imagePullSecrets:
  - name: harbor

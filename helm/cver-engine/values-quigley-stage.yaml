# Quigely test values for cver-engine

replicaCount: 1

nameOverride: "stage"

app:
  logLevel: "DEBUG"
  envName: "stage"
  debug: false
  cron:
    schedule:  "22 * * * *"
    suspend: true

test:
  test: true

image:
  repository: harbor.squid-ink.us/cver/cver-engine
  pullPolicy: Always
  tag: "stage-latest"

imagePullSecrets:
  - name: harbor
